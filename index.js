import 'react-native-gesture-handler';   // ← MUST be the very first import

import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('PreSenseAI', () => App);
